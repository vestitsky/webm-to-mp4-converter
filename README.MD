# Hi!

This script automatically converts webm videos to mp4 in the folder where you run it. 
To make it work, you need to install python and this module:

```
pip install moviepy
```

To run just do this:

```
python webm_to_mp4.py
```